# Legacy Flutter reference

The user changed the mobile stack to Kotlin native. This folder preserves the earlier Flutter work only. The active Android Studio Panda project is **../android**. See the repository README and docs/FIREBASE.md for current instructions. Flutter and Kotlin use different package IDs, local databases and backup formats; no automatic migration exists.
